const types = @import("types.zig");

pub var pos: types.Vec2 = types.Vec2{ .x = 0, .y = 0 };
pub var scale: f32 = 20;
pub var backgroundColour: types.Colour = types.Colour{ .r = 0.2, .g = 0.3, .b = 0.3 };
